@AGENTS.md

# 프로젝트 배경 (다른 컴퓨터에서 이어서 작업할 때 먼저 읽을 것)

## 이 프로젝트가 뭔지
`threads-hub`는 쿠팡파트너스(Coupang Partners) 제휴마케팅을 하는 1인 사용자를 위한 Threads 자동 발행 대시보드다. 사용자는 개발자가 아니다 — 코드를 못 읽고, 영어 UI 용어를 모르고, Railway/Meta 개발자 콘솔 등 낯선 관리자 콘솔을 혼자 못 다닌다. 항상 쉬운 한국어로, 화면을 경로(`/accounts` 같은)가 아니라 눈에 보이는 이름으로 설명할 것("왼쪽 메뉴의 '스레드 계정' 화면"처럼). 외부 콘솔(Railway, Meta, NCP 등) 설정은 한 번에 다 알려주지 말고 한 단계씩, 스크린샷 받아가며 진행할 것.

배포: Railway, `https://brave-intuition-production-9263.up.railway.app`. 단일 관리자 계정(AdminUser 1개, Settings 1행 공유) 구조 — 멀티테넌트 아님(아래 "보류된 계획" 참고).

## 핵심 기능
- OpenAI로 매일 "썰" 글 + 가끔(~40%) 쿠팡 상품 글 자동 생성
- AI가 검색 키워드를 제안해서 쿠팡 상품 링크 자동 수집(수동 검색 불필요)
- 선택적으로 Sora로 상품 영상 생성해서 첨부
- 실제 시계 기준으로 예약 발행(서버 재시작 기준 아님 — 테스트 기간 중 잦은 재배포에도 스케줄 안 깨지게 하려고)
- 여러 Threads 계정 연결 가능(현재 `the.sin44`, `sinkami66` 2개). 계정이 여러 개면 자동 생성 글도 계정마다 다른 본문+댓글로 발행됨

## 거절/보류된 기능 (다시 제안하지 말 것)
- 낯선 계정 자동 팔로우/좋아요/댓글 스팸 봇 — ToS 위반, 계정 정지 위험으로 거절
- 사용자 본인이 찍은 진짜 영상처럼 보이게 만드는 AI 영상 — 소비자기만/표시광고법 위험으로 거절. 티 나는 스타일의 AI 영상은 문제없고 이미 구현되어 있음
- 다른 판매자/크리에이터 영상 스크래핑 재게시 — 저작권 침해로 거절
- 네이버 데이터랩 실시간 검색어 연동 — 무료 API가 NCP 엔터프라이즈 상품으로 이전되면서 비용/절차 대비 실익이 없어 사용자가 직접 포기("그냥 접자"). 코드는 네이버 키 없으면 AI 추측 키워드로 자연스럽게 폴백함. 사용자가 다시 꺼내지 않는 한 안 건드림

## 코드 스타일/설계 관례
- 새 env var나 사용자 설정 단계를 늘리기보다 기존 설정값에서 파생시킬 것(예: 미디어 저장 경로는 `DATABASE_URL`에서, public base URL은 `threadsRedirectUri`에서 파생). 사용자가 Railway/Meta 콘솔 수동 설정을 여러 번 힘들게 거쳐서 "설정 추가 없는" 해결을 선호함
- `src/lib/ai.ts`의 AI 생성 글 스타일(첫 줄 "훅" 34자 제한, 줄바꿈으로 나눈 짧은 문단, 이모지/해시태그 금지)은 의도적으로 타이트하게 잡은 규칙이다. 예전에 "AI 글이 너무 딱딱하다"는 말을 문장 스타일 완화 요청으로 오해하고 풀었다가 사용자가 바로 되돌렸음 — 짧은 훅과 자연스러운 줄바꿈이 사람들을 스크롤 멈추게 하는 핵심이라 함. 비슷한 말이 나오면 문장 스타일이 아니라 소재/주제 다양성 얘기일 가능성이 높으니 먼저 뭐가 불만인지 확인할 것

## 두 번째 제품: 공구 허브 (공동구매 크리에이터 관리)

2026-08-20에 추가됨. **같은 저장소, 같은 코드를 Railway에 두 번 띄운 별개 사이트**다.

| | 스레드 사이트 | 공구 사이트 |
|---|---|---|
| Railway 서비스 | `-` (brave-intuition-production-9263) | `robust-nature` (robust-nature-production-5621) |
| 환경변수 | `APP_MODE=threads` | `APP_MODE=gonggu` |
| DB | 각자 볼륨 `/data/prod.db` | 각자 볼륨 `/data/prod.db` |
| 로그인 계정 | 별개 | 별개 |

두 서비스 모두 Railway 프로젝트 `protective-exploration` 안에 있다. `APP_MODE`는
`src/lib/app-mode.ts`가 읽고, 메뉴·첫 화면·설정 항목·스케줄러 on/off를 가른다.
값을 안 주면 전부 보이는 `full` 모드.

### 하는 일
공동구매를 함께할 크리에이터를 모아두고 진행 단계(후보→컨택중→확정→진행중→완료)와
실제 성과(상품별 공구 기록: 기간·수량·매출·지급액)를 기록한다. 상품도 따로 관리한다.

### 인스타 프로필 자동 입력 — 세 갈래
1. **크롬 확장 프로그램** (`extension/`) — 주력. 인스타 프로필의 팔로우 버튼 옆에
   "＋ 공구 허브에 담기"를 끼워 넣는다. 누르면 `/creators?add=1&handle=…&img=…&paste=…`로 연다
2. **즐겨찾기(북마클릿)** — `BookmarkletBox.tsx`가 만들어준다. 확장 프로그램 설치가
   부담스러울 때의 대안
3. **붙여넣기** — 프로필 화면을 복사해 붙여넣으면 OpenAI가 항목별로 나눈다
   (`src/lib/profile-paste.ts`). 인포크링크·"공구일정" 같은 신호를 보면 `공구진행중` 태그를 자동으로 단다

**공식 API(Business Discovery, `src/lib/instagram-discovery.ts`)는 코드만 있고 아직 연결 안 됨.**
연결하면 아이디만으로 팔로워 수·참여율까지 가져오지만, 내 인스타를 프로페셔널 계정으로
바꾸고 페이스북 페이지를 연결하는 메타 설정이 필요해서 미뤘다. 상대도 프로페셔널
계정이어야만 값을 준다.

### 알아둘 것
- **`javascript:` 를 소스에 글자 그대로 적으면 안 된다.** Turbopack이 해당 청크를 아예
  만들지 않아 그 페이지 전체가 안 움직인다. `BookmarkletBox.tsx`에서 조각내 이어붙인 이유
- 인스타 프로필 사진 주소는 시간이 지나면 만료된다. 안 열리면 조용히 감추게 해뒀다
- 화면은 2026-08-20부터 **흰 바탕 + 파랑**. 까만 화면이 눈에 부담된다고 해서 전면 교체했다.
  스레드 사이트도 같이 바뀐다(같은 코드)

### 아직 안 한 것 (셀러들이 쓰려면 필요)
회원가입, 회사별 데이터 분리, 결제, 개인정보처리방침. 크리에이터 연락처 같은 **남의
개인정보를 다루는 서비스**라 4번은 법적으로 필요하다. 혼자 한두 달 써보고 필요한 항목이
정리된 뒤에 붙이기로 함.

## 보류된 계획: 유료 멀티테넌트 서비스화
다른 사람도 돈 내고 쓰는 서비스로 만들 수 있는지 논의했었음. 필요한 것: (1) Meta App Review로 Threads 앱을 프로덕션 모드로 전환(가장 오래 걸림, 2-4주, 비즈니스 인증/개인정보처리방침/이용약관 URL 필요) (2) 멀티테넌트 데이터 분리(현재 AdminUser/Settings/ThreadsAccount/CoupangLink/Post가 전부 전역 공유) (3) 과금 방식 결정(사용자가 자기 OpenAI/쿠팡 키를 쓸지, 운영자가 부담할지) (4) 쿠팡파트너스 이용약관상 재판매가 괜찮은지 법적 확인(미확인, 사용자가 직접 체크해야 함). 합의된 순서는 Meta App Review 준비를 먼저 시작하면서 멀티테넌트 코드 작업을 병행하는 것이었으나, **아직 착수 전** — 2번째 계정(sinkami66) 추가 작업 때문에 보류됨.

## 최근 작업 흐름 (git log 참고)
2026-08-15~16에 걸쳐: 연결설정 3탭을 1화면으로 통합 → 대시보드를 계정별 시간대 히트맵 히어로로 재구성 → 댓글(쿠팡 링크) 발행 실패가 화면에 안 보이던 버그 수정 → 조회수 새로고침 버튼이 대시보드 숫자를 안 바꾸던 버그 수정 → 2번째 계정 연결 및 계정별 다른 본문/댓글 자동 생성 → 조회수 새로고침 결과 표시 → 계정 연결 실패 원인 화면 표시 → 대시보드 조회수 1분마다 자동 새로고침(가장 최근, 버튼/알림창 없이). 2026-08-20에는 공동구매 크리에이터 관리 화면 추가 → 인스타 프로필 자동 입력(확장 프로그램·즐겨찾기·붙여넣기) → 공구 사이트 별도 배포 → 흰 화면으로 전환. 최신 상태 파악은 `git log --oneline -20`으로 확인.

